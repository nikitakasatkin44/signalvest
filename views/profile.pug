doctype html
html(lang="en")
    head
        title Профиль
        link(href="css/bootstrap.css" rel="stylesheet")
        link(href="css/profile.css" rel="stylesheet")

    body
        .container
            .text-center
                h1.pb-3 Личный профиль
                a.btn.btn-danger.btn-lg.mr-2(href="/logout") Выйти
                a.btn.btn-info.btn-lg(href="/") На главную

            .row
                .col-sm-8
                    .card.mt-3
                        h3 Учетная запись
                        p Электронная почта: #{user.local.email}

                        if user.local.login
                            p Логин: #{user.local.login}
                        if !user.local.login
                            p Логин: не заполнен

                        if user.local.phone
                            p Телефон: #{user.local.phone}
                        if !user.local.phone
                            p Телефон: не заполнен

                        if user.local.regDate
                            p Зарегистрирован: #{regDate}
                        if !user.local.regDate
                            p Дата регистрации: не указана

                        if user.local.role
                            p Роль: #{user.local.role}

                        //form(action="/define" method="POST")
                        //    .form-inline.pl-4
                        //        .form-check
                        //            label.radio-inline(for="adminCheck") Admin &nbsp
                        //                input#adminCheck(type="radio" name="defineRole" value="admin")
                        //            label.radio-inline.ml-5(for="guestCheck") Guest &nbsp
                        //                input#guestCheck(type="radio" name="defineRole" value="guest")
                        //            button.btn.btn-primary.ml-5 Submit












